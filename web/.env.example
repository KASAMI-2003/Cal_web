VITE_RUST_API_ORIGIN=http://127.0.0.1:8088
VITE_PYTHON_API_ORIGIN=http://127.0.0.1:3569
# 终端 WebSocket 默认经 Vite 代理到 Python（见 vite.config `/api/ssh/ws`）。若 Python 占用改用其它端口，需在本文件与启动 Vite 的环境里同步 TERMINAL_WS_PORT（与 pyserver 一致）。
# TERMINAL_WS_PORT=8765
