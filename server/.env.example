# 复制为 server/.env 或在启动前 export（pyserver 不自动加载本文件，需自行注入环境）
# 本地开发常见配置：

# Materials Project（检索 /api/data、page2_search；无 key 时 MP 分支不可用）
# MP_API_KEY=your_mp_api_key

# MySQL（与 pyserver.py 中连接配置一致）
# MYSQL_HOST=127.0.0.1
# MYSQL_USER=py_server
# MYSQL_PASSWORD=123456

# HTTP / WebSocket 端口（默认 3569 / 8765，冲突时 pyserver 会自动顺延）
# PY_HTTP_PORT=3569
# TERMINAL_WS_PORT=8765

# 无 cargo / 不启 Rust 用户服务时
# TSX_SKIP_RUST_SERVER=1

# 数字孪生 HTEM（源码目录，非 pip 包；必装才能生成 SAM 各向异性曲面）
# 默认路径：server/digital_twin/HTEM-main（可用 scripts/setup_htem.sh 安装最小运行时）
# HTEM_ROOT=/path/to/HTEM-main
